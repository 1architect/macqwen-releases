# Documentation archive

This directory preserves evidence that no longer belongs in current guidance.

- `chat/` contains the completed restructure plan.
- `qwen27b/` contains dated sessions, detailed findings, and old runbooks.
- `flashnext/` contains the original briefing, handoff, and streaming study.

Archived files can contain old paths, commands, model locations, and measured
results. Use the active brief and handoff before running a command.
