# Security policy

## Supported versions

Only the latest release receives security fixes.

## Report a vulnerability

Use GitHub private vulnerability reporting for sensitive reports.

Do not open a public issue with credentials, prompt data, or session files.

## Sensitive local data

FlashNext does not require API keys.

Saved sessions can contain private prompt state.
Keep `~/.cache/flashnext/sessions` private.
